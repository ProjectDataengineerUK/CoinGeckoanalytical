name: bronze-migration

on:
  workflow_dispatch:
    inputs:
      confirm_migration:
        description: "Set to true to run the Bronze schema remediation job."
        required: false
        type: boolean
        default: false

permissions:
  id-token: write
  contents: read

jobs:
  dev-migration:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && inputs.confirm_migration
    env:
      DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
      ARM_CLIENT_ID: ${{ secrets.DATABRICKS_SP_CLIENT_ID }}
      ARM_CLIENT_SECRET: ${{ secrets.DATABRICKS_SP_CLIENT_SECRET }}
      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deploy dependencies
        run: python3 -m pip install --upgrade pip pyyaml

      - name: Install Databricks CLI
        run: |
          curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | sh
          databricks version

      - name: Check migration prerequisites
        id: prereq
        run: |
          ready=true
          if [ -z "$DATABRICKS_HOST" ] || [ -z "$ARM_CLIENT_ID" ] || [ -z "$ARM_CLIENT_SECRET" ]; then
            echo "Missing Databricks credentials (DATABRICKS_HOST, DATABRICKS_SP_CLIENT_ID, DATABRICKS_SP_CLIENT_SECRET); skipping migration."
            ready=false
          elif ! command -v databricks >/dev/null 2>&1; then
            echo "Databricks CLI not installed; skipping migration."
            ready=false
          fi
          echo "migration_ready=$ready" >> "$GITHUB_OUTPUT"

      - name: Validate bundle
        if: steps.prereq.outputs.migration_ready == 'true'
        run: |
          cd databricks
          python3 validate_bundle.py
          databricks bundle validate

      - name: Deploy bundle
        if: steps.prereq.outputs.migration_ready == 'true'
        run: |
          cd databricks
          databricks bundle deploy -t dev

      - name: Run Bronze migration
        if: steps.prereq.outputs.migration_ready == 'true'
        run: |
          cd databricks
          databricks bundle run bronze_market_table_migration_job -t dev
